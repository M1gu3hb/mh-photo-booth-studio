/** Photos per session — the app supports 2, 3 or 4 (CAMERA_CAPTURE.md). */
export const PHOTO_COUNT_OPTIONS = [2, 3, 4] as const;

/** Sane upper bound for default copies in an event config. */
export const MAX_DEFAULT_COPIES = 20;
